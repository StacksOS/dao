import { Contract } from '@clarigen/core';
import type { TokenContract } from './types';
export type { TokenContract } from './types';
export declare const tokenContract: (provider: BaseProvider) => any;
export declare const tokenInfo: Contract<TokenContract>;
